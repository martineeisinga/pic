
<div class="book-trip">

  <h1><%= @trip.name %></h1>
  <h2><%= @trip.location %></h2>

  <div class="card" style="background-image: url(<%= cl_image_path @trip.photo%>);">
  </div>

  <div class="container">
    <div class="row">
      <div class="col-xs-12 col-sm-offset-3 col-sm-6 col-md-offset-3 col-md-6">
        <div class="book-trip-form-wrapper text-center">
          <%= simple_form_for [@trip, @booking], input_html: { id: "trip-form" } do |f| %>
          <%= f.input :check_in, as: :date, html5: true  %>
          <%= f.input :check_out, as: :date, html5: true  %>
          <%#= f.input :guests, collection: 1..10 %>
          <%= f.input :guests, as: :integer %>
          <%= f.button :submit, class: "btn btn-primary" %>
          <% end %>
          <%= render "shared/flashes" %>
        </div>
      </div>
    </div>
  </div>
</div>

<%= render partial: "shared/floating_button" %>

